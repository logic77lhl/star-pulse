# 复制为 .env 后填写。.env 已被 .gitignore 忽略，不会提交。
#
# ── 1. GitHub Token（强烈建议配，否则只有 60 次/小时，采不了几个仓库）──────
# 生成地址：https://github.com/settings/personal-access-tokens/new
# 类型选 Fine-grained，权限只需：Public Repositories -> Metadata: Read
# 说明：如果不填，程序会自动降级为匿名模式，并在候选池超过 40 个时拒绝运行。
GITHUB_TOKEN=

# ── 2. LLM（可选，不填则跳过中文解读，榜单与数据功能完全不受影响）─────────
# 仅用于给 Top N 项目写中文一句话和本期综述。
# 重要：数值、排名、增量永远由 SQL 计算，LLM 不参与，避免幻觉。
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=
